# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/lucida-openephyra" ]; then
		update-rc.d lucida-openephyra defaults >/dev/null
		invoke-rc.d lucida-openephyra start || exit $?
	fi
fi
# End automatically added section
